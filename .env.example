# Copy to .env and adjust if needed. The defaults match docker-compose.yml.

# PostgreSQL connection string used by the API (Drizzle).
DATABASE_URL=postgresql://timesheet:timesheet@localhost:5433/timesheet
